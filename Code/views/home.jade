extends layout
block content
  div#allmap
    script
      //var map = new BMap.Map("allmap");            // 创建Map实例
      var map = new $Map("allmap").map;
      var point = new BMap.Point(116.404, 39.915);    // 创建点坐标
      map.centerAndZoom(point,16);                     // 初始化地图,设置中心点坐标和地图级别。
  div.hero-unit.container
    h1= title
    p Welcome to #{title}
