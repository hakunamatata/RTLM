extends layout
block content
  div#mapmask
  div#allmap
  script
    var Map = new $Map("allmap"),
        map = Map.map;
    var point = new BMap.Point(116.404, 39.915);    // 创建点坐标
    map.centerAndZoom(point,16);                     // 初始化地图,设置中心点坐标和地图级别。
  div.hero-unit.container
    h1= title
    p Welcome to #{title}

    script
      var nav = new UI.Navbar(".navbar");
      $('#aDisappear').click(function(){ nav.hide(function(){Map.show();}); });
